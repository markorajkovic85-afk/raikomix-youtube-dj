# ──────────────────────────────────────────────────────────────────
# RaikoMix YouTube DJ — Environment Variables
# Copy this file to .env.local and fill in your values.
# Vite loads .env.local automatically; never commit real keys.
# ──────────────────────────────────────────────────────────────────

# YouTube Data API v3 key (required for playlist import)
# Without this key the app falls back to Invidious proxy for search,
# which is slower and subject to instance availability.
# Get one at: https://console.cloud.google.com → APIs → YouTube Data API v3
VITE_YOUTUBE_API_KEY=your_youtube_api_key_here

# Google Gemini API key (optional — enables AI BPM/key detection)
# Without this key the BPM and musical key fields show default values.
# Get one at: https://aistudio.google.com/app/apikey
VITE_API_KEY=your_gemini_api_key_here
